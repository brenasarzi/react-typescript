import React from "react";

export class Botao extends React.Component{
    render(){
        return(
            <button style={{
                backgroundColor: "pink"
            }}>
                Botão
            </button>
        )
    }
}
